<%= form_tag(search_path, method: :get) do %> #search_path here is the name you used for
<%= text_field_tag(:type, params[:type]) %> #the route in the routes.rb file
<%= submit_tag "Search" %>
<% end %>
